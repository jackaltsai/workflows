<section>
    <div class="container">

        <% users.forEach(function(user){ %>
            
                <h1><%=user.name%></h1>
                <h1><%=user.email%></h1>
                <h1><%=user.message%></h1>

        <% }) %>


    </div>
</section>